visible = true;
if (!variable_global_exists("paused")) global.paused = false;
pausedSurf = -1;   // holds frozen frame
btn_gx = -1;       // GUI X of resume icon when paused
btn_gy = -1;       // GUI Y of resume icon when paused
